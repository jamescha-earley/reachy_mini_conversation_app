# TTS Server requirements - Apple Silicon (MLX backend)
#
# Setup:
#   python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# For CUDA/Docker deployments, use requirements-cuda.txt instead.
# qwen-tts and mlx-audio pin conflicting transformers versions and cannot coexist.

# --- Core server ---
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6      # For file upload endpoints

# --- Audio processing ---
numpy>=1.24.0
soundfile>=0.12.0             # WAV file I/O
librosa>=0.10.0               # Pitch shifting

# --- TTS backend: MLX (Apple Silicon) ---
mlx-audio>=0.3.0
